name: GPT Code Critic Analyzer
description: An action that analyzes code changes with GPT Code Critic and uploads the results
inputs:
  OPENAI_API_KEY:
    description: "The API key for OpenAI ChatGPT"
    required: true
    default: ${{ secrets.OPENAI_API_KEY }}
